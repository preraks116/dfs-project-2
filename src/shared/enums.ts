
/**
 * @description Enum for the different types of buttons
 * @export enum
 * @enum {string}
 */
export enum THEMES {
  LIGHT = "light",
  DARK = "dark",
}

/**
 * @description Enum for the different image sizes
 * @export enum
 * @enum {string}
 */
export enum ImageSize {
  DEFAULT = "default",
  FUll = "full",
  SMALL = "small",
  MEDIUM = "medium",
  XS = "xs",
}

/**
 * @description Enum for the different types of buttons
 * @export enum
 * @enum {string}
 */
export enum NavbarType {
  DEFAULT = "default",
  CENTERED = "centered",
}

/**
 * @description Enum for the different types of text align
 * @export enum
 * @enum {string}
 */
export enum TextAlign {
  LEFT = "left",
  RIGHT = "right",
  CENTER = "center",
  JUSTIFY = "justify",
  NONE = "unset",
}

/**
 * @description Enum for the different types of logos
 * @export enum
 * @enum {string}
 */
export enum LogoType {
  TEXT = "text",
  IMAGE = "image",
}

/**
 * @description Enum for the different types of lists
 * @export enum
 * @enum {string}
 */
export enum ListType {
  disc = "list-disc",
  number = "list-decimal",
  none = "list-none",
}
